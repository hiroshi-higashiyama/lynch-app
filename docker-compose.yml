services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_DEBUG=0
      - EDINET_API_KEY=${EDINET_API_KEY:-}
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
    restart: unless-stopped
